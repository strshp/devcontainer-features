{
    "name": "GitHub CLI",
    "id": "gh",
    "version": "1.2.1",
    "description": "Persists GitHub CLI authentication credentials across container rebuilds and different DevContainers. Bind-mounts the host's ~/.config/gh and points gh at it via GH_CONFIG_DIR, so gh auth login only needs to be done once on the host. Installs the GitHub CLI automatically via the github-cli feature.",
    "mounts": [
        {
            "source": "${localEnv:HOME}/.config/gh",
            "target": "/var/gh-host-config",
            "type": "bind"
        }
    ],
    "containerEnv": {
        "GH_CONFIG_DIR": "/var/gh-host-config"
    },
    "postCreateCommand": "sudo -n /usr/local/bin/gh-init",
    "dependsOn": {
        "ghcr.io/devcontainers/features/github-cli:1": {}
    }
}
