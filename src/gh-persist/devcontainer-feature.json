{
    "name": "GitHub CLI Credential Persistence",
    "id": "gh-persist",
    "version": "1.0.0",
    "description": "Persists GitHub CLI authentication credentials across container rebuilds and different DevContainers. Bind-mounts the host's ~/.config/gh so that gh auth login only needs to be done once on the host.",
    "mounts": [
        {
            "source": "${localEnv:HOME}/.config/gh",
            "target": "/var/gh-host-config",
            "type": "bind"
        }
    ],
    "postCreateCommand": "sudo -n /usr/local/bin/gh-persist-init"
}
